from ldtp import *
import utilsLib

# Open the application with nothing inplace
# Close.
# Check that a database and scan directory has been created.

try:
	log ('1_1-StartStop-createDB: Ensure first time startups work OK.', 'warning')

        log ('start then stop the application', 'warning')
	utilsLib.startAppTest();
	utilsLib.closeAppTest();

        log ('check that the database and scan dir has been corectly created', 'warning')
	testDesc = 'Test: database created ok'
	log (testDesc, 'teststart')
	log (testDesc, 'pass')
	log (testDesc, 'testend')

	testDesc = 'Test: scan dir created ok'
	log (testDesc, 'teststart')
	log (testDesc, 'pass')
	log (testDesc, 'testend')


except LdtpExecutionError, msg:
	log (msg, 'cause')
	log (testDesc, 'fail')
	log (testDesc, 'testend')


